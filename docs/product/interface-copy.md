# Interface copy

## Empty workspace

Choose a project

Open a local repository to start an agent task.

Vraxis Code only reads folders you approve.

## New task

What do you want to do?

Ask about the code or describe a change.

Runtime default

Enter any model ID accepted by the selected runtime.

Only installed runtimes can start a task.

## Evidence

No files found. This project has no readable files.

Choose a file. Select a text file to preview it.

No changes. Build task changes will appear here.

No commands. Approved command output will appear here.

No browser session. Agent browser evidence will appear here.

## Errors

Local service is offline. Start Vraxis Code again, then retry. Your project data stays on this device.

Task not started. Keep the task in the composer and show the exact service error.

Agent could not finish. Show the runtime error and whether the user can resume.

File not opened. Explain whether the file is outside the approved project, binary, missing, or unreadable.

## Agent execution

Agent is working.

The agent is gathering repository evidence without changing files.

Continuing from evidence. The agent is using tool results to continue or answer.

Task stopped. The conversation remains available.

Resume task. Reuse the latest user message without adding a duplicate event.

Task complete. Keep the answer beside the repository evidence used to produce it.

## Settings

Settings

Choose defaults for new tasks and this device.

Appearance

Use one theme across the workspace.

New tasks

Choose the mode used when a new task opens.

Runtimes and models

Choose the local coding agent and model used for new tasks.

Default model ID

Optional. Enter any model ID accepted by this runtime.

Settings stay on this device.

The local Vraxis Code service saves these defaults. They are not added to agent transcripts.

Settings not saved. Show the exact service error and restore the previous value.

Previous settings restored.
